import {Component} from '@angular/core';

@Component({
  selector: 'app-root',
  styleUrls: ['app.css'],
  template: `
    <div style="font-family:'serif','Times New Roman'; font-size:30px" contentEditable="true">string</div>
    <button type="button" class="btn btn-primary" (click)="true">
      bigger
    </button>
  `,
})
export class App {
  textSize =24;
}

ZZ
